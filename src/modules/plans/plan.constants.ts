// Plan type enum for the plan module
export enum PlanType {
    BASIC = 'BASIC',
    STANDARD = 'STANDARD',
    PREMIUM = 'PREMIUM'
}

// Plan status enum
export enum PlanStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}

// Plan duration enum
export enum PlanDuration {
    MONTHLY = 'MONTHLY',
    YEARLY = 'YEARLY'
}
