// Import all models from the models folder.
import { UserModel, User } from './user.model';
// Add additional model imports here as needed, e.g., OTPModel.

// Export all models.
export { UserModel, User };
